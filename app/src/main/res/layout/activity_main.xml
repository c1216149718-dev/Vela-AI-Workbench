<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navigation_drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <eightbitlab.com.blurview.BlurTarget
            android:id="@+id/blur_target"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.fragment.app.FragmentContainerView
                android:id="@+id/nav_host_container"
                android:name="androidx.navigation.fragment.NavHostFragment"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:defaultNavHost="true"
                app:navGraph="@navigation/main_nav_graph" />
        </eightbitlab.com.blurview.BlurTarget>

        <com.google.android.material.navigationrail.NavigationRailView
            android:id="@+id/navigation_rail"
            android:layout_width="80dp"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="@color/app_background"
            android:visibility="gone"
            app:itemIconTint="@color/bottom_nav_item_color"
            app:itemTextColor="@color/bottom_nav_item_color"
            app:labelVisibilityMode="unlabeled"
            app:menu="@menu/bottom_nav_menu" />

        <FrameLayout
            android:id="@+id/side_menu_handle_target"
            android:layout_width="48dp"
            android:layout_height="56dp"
            android:layout_gravity="end|center_vertical"
            android:contentDescription="@string/side_tools_open"
            android:focusable="true">

            <androidx.appcompat.widget.AppCompatImageButton
                android:id="@+id/side_menu_button"
                android:layout_width="48dp"
                android:layout_height="56dp"
                android:background="@drawable/side_menu_handle_background"
                android:clickable="false"
                android:elevation="2dp"
                android:focusable="false"
                android:importantForAccessibility="no"
                android:paddingStart="9dp"
                android:paddingTop="16dp"
                android:paddingEnd="21dp"
                android:paddingBottom="16dp"
                android:src="@drawable/ic_side_handle"
                android:translationX="35dp" />
        </FrameLayout>

        <eightbitlab.com.blurview.BlurView
            android:id="@+id/bottom_nav_blur"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:layout_gravity="bottom"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/bottom_nav_glass"
            android:clipToOutline="true"
            android:elevation="10dp"
            app:blurOverlayColor="@color/bottom_nav_blur_overlay">

            <View
                android:id="@+id/bottom_nav_indicator"
                android:layout_width="50dp"
                android:layout_height="40dp"
                android:layout_gravity="start|top"
                android:layout_marginTop="7dp"
                android:background="@drawable/bottom_nav_liquid_indicator"
                android:importantForAccessibility="no" />

            <com.google.android.material.bottomnavigation.BottomNavigationView
                android:id="@+id/bottom_nav"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                android:elevation="0dp"
                android:paddingTop="3dp"
                android:paddingBottom="3dp"
                app:itemBackground="@android:color/transparent"
                app:itemHorizontalTranslationEnabled="false"
                app:itemIconSize="23dp"
                app:itemIconTint="@color/bottom_nav_item_color"
                app:itemRippleColor="@color/card_pressed"
                app:itemTextColor="@color/bottom_nav_item_color"
                app:labelVisibilityMode="unlabeled"
                app:menu="@menu/bottom_nav_menu" />
        </eightbitlab.com.blurview.BlurView>

        <androidx.compose.ui.platform.ComposeView
            android:id="@+id/launch_entry_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clickable="true"
            android:elevation="32dp"
            android:focusable="true"
            android:visibility="gone" />
    </FrameLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/side_navigation"
        android:layout_width="288dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="@drawable/vela_drawer_background"
        android:elevation="12dp"
        app:headerLayout="@layout/navigation_drawer_header"
        app:itemIconPadding="16dp"
        app:itemIconSize="24dp"
        app:itemIconTint="@color/side_nav_item_color"
        app:itemRippleColor="@color/card_pressed"
        app:itemTextColor="@color/side_nav_item_color"
        app:menu="@menu/side_nav_menu" />
</androidx.drawerlayout.widget.DrawerLayout>
