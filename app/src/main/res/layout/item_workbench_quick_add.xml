<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="20dp"
    android:paddingTop="16dp"
    android:paddingEnd="20dp">

    <EditText
        android:id="@+id/wb_quick_input"
        android:layout_width="0dp"
        android:layout_height="44dp"
        android:layout_weight="1"
        android:background="@drawable/workbench_input_background"
        android:hint="@string/workbench_quick_add_hint"
        android:importantForAutofill="no"
        android:inputType="text"
        android:maxLength="120"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:singleLine="true"
        android:textColor="@color/balance_text"
        android:textColorHint="@color/muted_text"
        android:textSize="14sp" />

    <ImageView
        android:id="@+id/wb_quick_btn"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/workbench_action_background"
        android:contentDescription="@string/workbench_quick_add_today"
        android:padding="12dp"
        android:src="@drawable/ic_module_plan"
        app:tint="@color/on_hero" />
</LinearLayout>
