# Design

- `icon/`：正式 App 图标母版与品牌图形。
- `concepts/`：可继续编辑的设计探索、参考资料和阶段渲染。
- `validation/<version>/`：按版本存放的设备验收截图。
- `validation/historical/`：无法可靠归属到单一版本的历史截图。

新版本不要把图片散放在 `design/` 根目录；请先创建对应版本文件夹。

当前陶瓷自适应图标母版与用户参考位于 `icon/v1.19.0/`；当前设备验收证据位于 `validation/v1.19.0/`。
